{
  "name": "capstone",
  "version": "1.0.0",
  "bin": {
    "antigravity": "./gravity.js"
  },
  "description": "TaskForge - Shape ideas into results",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "start": "concurrently \"npm run server:start\" \"npm run client:start\"",
    "server:dev": "npm --prefix server run dev",
    "server:start": "npm --prefix server start",
    "client:dev": "npm --prefix client run dev -- --host 0.0.0.0",
    "client:build": "npm --prefix client run build",
    "client:start": "npm --prefix client run build && npm --prefix client run preview",
    "prisma:generate": "npm --prefix server run prisma:generate",
    "prisma:push": "npm --prefix server run prisma:push",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pretty": "prettier --write \"./**/*.{js,jsx,ts,tsx,json,css,md}\""
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.13.5",
    "concurrently": "^9.2.1",
    "lucide-react": "^0.561.0",
    "prisma": "7.1.0",
    "react-quill-new": "^3.8.3"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
